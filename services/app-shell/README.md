# App Shell API

`services/app-shell/` is the deployable backend for auth, sessions, workspaces, and collaborators.
Its runtime, HTTP, and migration code now live under `services/app-shell/internal/`.
