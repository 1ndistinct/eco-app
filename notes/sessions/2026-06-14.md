## 23:25 BST
- Context: Nicole's birthday gallery needed another batch of photos, stable descriptions for the new
  additions, and a refreshed Frank icon choice from two new source images.
- Action: Copied the newly added `nicole/*.JPG` shots into
  `services/web-nicole/src/assets/library/`, expanded the Nicole and shell Vite asset handling to
  accept uppercase image extensions, seeded specific captions for each new photo, added a
  deterministic fallback caption generator for the rest of the library, and regenerated the shell
  PNG icon set from `services/web/public/frankicon-square.jpg`.
- Outcome: The Nicole library now includes the new museum, sunlit, and mirrored-room photos in the
  shuffle pool with fixed descriptions, and the shell icon assets now use the tighter square Frank
  crop across favicon/PWA/logo fallbacks. `task test:frontend` and `task build:frontend` passed.
- References: `services/web-nicole/src/data.ts`, `services/web-nicole/vite.config.ts`,
  `services/web/vite.config.ts`, `services/web/public/`, `task test:frontend`,
  `task build:frontend`

## 23:26 BST
- Context: The updated shell and Nicole remote needed to go live on `tm` without rebuilding the
  untouched backend and todo services.
- Action: Synced the changed `services/web` and `services/web-nicole` files to `tm`, ran
  `task docker:push:web` and `task docker:push:web-nicole` there, then restarted
  `deployment/app-web` and `deployment/app-web-nicole` in `app-system`.
- Outcome: Both frontend deployments rolled out successfully on `tm`. Public probes returned `200`
  for `https://eco.treehousehl.com/` and `https://eco.treehousehl.com/nicole/remoteEntry.js`.
  The shell origin serves the new `frankicon-192x192.png` payload at 76,329 bytes, while the
  public CDN edge was still caching the older filename at the time of verification.
- References: `task docker:push:web`, `task docker:push:web-nicole`,
  `kubectl rollout status deployment/app-web -n app-system`,
  `kubectl rollout status deployment/app-web-nicole -n app-system`,
  `curl https://eco.treehousehl.com/manifest.webmanifest`
