# 2026-06-13

## 11:22 BST
- Context: Build a birthday-themed federated workspace app for Nicole using local photos from
  `nicole/`, link out to Nicole's public LinkedIn and Behance pages, and deploy it through the
  existing `eco-app` shell onto the `tm` environment.
- Action: Added a new `services/web-nicole` Module Federation remote, shell app-selection state,
  app-aware workspace routing, Nicole-specific ingress rules, and Docker/Helm/Tilt task wiring.
- Outcome: The shell now mounts both the default todo remote and the Nicole birthday remote, with
  `/nicole/workspaces/:workspaceId` kept on the shell while `/nicole/remoteEntry.js` and
  `/nicole/assets/*` are served by `web-nicole`.
- References: `services/web/`, `services/web-nicole/`, `Taskfile.yml`,
  `deploy/helm/app/templates/app-ingress.yaml`

## 11:22 BST
- Context: The new Nicole remote built successfully, but Module Federation DTS generation still
  warned on imported image assets.
- Action: Added ambient image module declarations in
  `services/web-nicole/src/types/assets.d.ts` and removed an accidental `packageManager` field
  from `services/web-nicole/package.json`.
- Outcome: `task build:frontend` completed cleanly for `services/web`, `services/web-nicole`, and
  `services/web-todo`, and the Nicole remote now reports `Federated types created correctly`.
- References: `services/web-nicole/src/types/assets.d.ts`, `services/web-nicole/package.json`,
  `task build:frontend`

## Next
- Add authenticated end-to-end coverage for opening the Nicole app after login with a real
  workspace UUID.
- Decide whether `tm` should add `~/.local/bin` to the non-interactive PATH for future remote
  automation runs.

## 11:34 BST
- Context: The first `tm` deploy attempts built the new images successfully but failed before the
  release completed because the frontend Docker builds picked up pnpm 11 on `tm`, and the box did
  not have `helm` installed.
- Action: Pinned the frontend Docker build steps to `pnpm@9.15.1` with `corepack prepare`, then
  installed Helm `v3.21.1` manually into `~/.local/bin/helm` on `tm` after the official installer
  required interactive `sudo`.
- Outcome: `task deploy:docker:tm` succeeded from `tm`, Helm upgraded release `app` to revision
  `42`, and all deployments rolled out successfully: `app-app`, `app-todo-api`, `app-web`,
  `app-web-nicole`, and `app-web-todo`.
- References: `services/web/Dockerfile`, `services/web-nicole/Dockerfile`,
  `services/web-todo/Dockerfile`, `task deploy:docker:tm`

## 11:35 BST
- Context: The deployment needed host-side verification to confirm the ingress split behaved the
  way the Nicole app expects.
- Action: Probed the live host paths over HTTP.
- Outcome: `http://eco.treehousehl.com/api/healthz` returned `200`, the site root returned shell
  HTML, `http://eco.treehousehl.com/nicole/remoteEntry.js` returned `200
  application/javascript`, and `http://eco.treehousehl.com/nicole/workspaces/test` returned the
  shell HTML document rather than the remote asset host.
- References: `curl http://eco.treehousehl.com/api/healthz`,
  `curl http://eco.treehousehl.com/nicole/remoteEntry.js`,
  `curl http://eco.treehousehl.com/nicole/workspaces/test`

## 11:50 BST
- Context: The live Nicole page loaded again after the Module Federation fix, but the layout still
  felt broken in the shell: scrolling was constrained and the photo collage cropped the images too
  aggressively.
- Action: Reworked `services/web-nicole/src/exposed/birthdayFeature.css` so the Nicole remote owns
  its own scroll container inside the shell, photo cards use natural aspect-ratio-based tiles
  instead of row-span crops, captions sit below images instead of over dark overlays, and Behance
  cards now use `object-fit: contain`.
- Outcome: Rebuilt and redeployed only `web-nicole`, rolled `app-web-nicole`, and confirmed the
  new CSS asset was live at `http://eco.treehousehl.com/nicole/assets/BirthdayFeature-lEKh7Fyx.css`.
- References: `services/web-nicole/src/exposed/birthdayFeature.css`,
  `task docker:push:web-nicole`, `kubectl rollout status deployment/app-web-nicole -n app-system`

## 11:59 BST
- Context: A later pull from `main` left the local repo in a merge state, and the Helm chart still
  did not describe the live Postgres persistence that `tm` was already relying on.
- Action: Resolved the merge conflicts in `services/web/Dockerfile`,
  `services/web-todo/Dockerfile`, and `services/web/src/App.tsx`, then added PVC-backed Postgres
  storage to `deploy/helm/app` with an `existingClaim` override for environments that already have
  a bound volume.
- Outcome: The merge is conflict-free and ready to conclude with a merge commit. `task
  helm:template K3D_VALUES_FILE=./deploy/k3d/tm.values.yaml` now renders a Postgres deployment that
  mounts `/var/lib/postgresql/data` from claim `app-postgres`, matching the live `tm` cluster
  without asking Helm to recreate the PVC.
- References: `services/web/Dockerfile`, `services/web-todo/Dockerfile`,
  `services/web/src/App.tsx`, `deploy/helm/app/templates/postgres-deployment.yaml`,
  `deploy/helm/app/templates/postgres-pvc.yaml`, `deploy/k3d/tm.values.yaml`, `task helm:template`

## 12:09 BST
- Context: The live mobile workspace picker showed `Personal · ciaranmckey@gmail.com` twice even
  though only one personal workspace should exist.
- Action: Queried the live Postgres data on `tm`, confirmed there was one `workspaces` row plus one
  invalid self-membership row for the same owner, then patched both Postgres stores to exclude
  owned workspaces from the collaborator branch, added a cleanup migration to delete owner
  self-memberships, and added a frontend session normalizer test that collapses duplicate workspace
  IDs defensively.
- Outcome: Targeted Go and Vitest suites passed locally. After rebuilding and redeploying `app`,
  `todo-api`, and `web` on `tm`, Helm release `app` reached revision `45`, the self-membership row
  for `ciaranmckey@gmail.com` was removed, and the effective accessible-workspace query now returns
  a single `Personal` workspace row for that user.
- References: `services/app-shell/internal/httpapi/postgres_store.go`,
  `services/app-todo/internal/httpapi/postgres_store.go`,
  `services/app-shell/internal/migrations/shell/migrations/20260613121500_remove_owner_workspace_memberships.sql`,
  `services/app-todo/internal/migrations/shell/migrations/20260613121500_remove_owner_workspace_memberships.sql`,
  `services/web/src/app/session.ts`, `services/web/src/app/session.test.ts`,
  `task docker:push:app`, `task docker:push:todo-api`, `task docker:push:web`

## 13:14 BST
- Context: Nicole needed to be re-added to the live app as a real provisioned user.
- Action: Confirmed the Nicole frontend route was still deployed on `tm`, provisioned
  `thenicoleborman@gmail.com` through `task user:create:cluster`, queried the resulting personal
  workspace ID from Postgres, and verified live login plus session bootstrap against
  `https://eco.treehousehl.com`.
- Outcome: The new user can authenticate successfully, `passwordResetRequired` is still `true` for
  first login, and the session returns one personal workspace owned by
  `thenicoleborman@gmail.com`.
- References: `Taskfile.yml`, `services/app-shell/scripts/create-cluster-user.sh`,
  `https://eco.treehousehl.com/api/auth/login`,
  `https://eco.treehousehl.com/nicole/workspaces/27e9cba5-7fc6-4183-bc87-634ae45072f5`

## 13:21 BST
- Context: The shell still treated the todo remote as the implicit app when users opened the bare
  site root, but the desired first impression is now the Nicole remote.
- Action: Added a dedicated Nicole landing app constant in the shell frontend and used it only for
  the root-path bootstrap case, while keeping explicit `/todo/workspaces/:workspaceId` routes and
  most non-routing tests pinned to their own explicit app paths.
- Outcome: Web tests now cover the root-path Nicole landing behavior separately from unrelated app
  flows, and opening `/` after session bootstrap will route to
  `/nicole/workspaces/<workspace-id>` by default.
- References: `services/web/src/app/workspaceApps.ts`, `services/web/src/App.tsx`,
  `services/web/src/App.test.tsx`, `pnpm --dir services/web test -- --run`

## 13:44 BST
- Context: The live app switcher still listed Todos above Nicole in the sidebar, which worked
  against the Nicole-first landing and visual hierarchy.
- Action: Reordered `WORKSPACE_APPS` so Nicole renders before Todos everywhere that consumes the
  shared app list, and tightened the desktop sidebar test to assert Nicole appears above the Todos
  button.
- Outcome: The shell frontend still passes its full unit suite, and the next `app-web` rollout will
  present Nicole above Todos in the sidebar and mobile app list.
- References: `services/web/src/app/workspaceApps.ts`, `services/web/src/App.test.tsx`,
  `pnpm --dir services/web test -- --run`

## 14:04 BST
- Context: The Nicole remote needed a real background track, but the first-load behavior still had
  to respect browser autoplay restrictions.
- Action: Added `Arabella.mp3` to the Nicole remote assets, wired the birthday hero to try
  autoplay on mount, retry after the first user interaction if blocked, and expose an inline
  play/pause control with a small Vitest coverage update.
- Outcome: The remote now starts the track automatically where the browser allows it, falls back to
  a visible start control where it does not, and stops playback when the Nicole remote unmounts.
- References: `services/web-nicole/src/exposed/BirthdayFeature.tsx`,
  `services/web-nicole/src/exposed/birthdayFeature.css`,
  `services/web-nicole/src/exposed/BirthdayFeature.test.tsx`,
  `services/web-nicole/src/assets/audio/Arabella.mp3`

## 14:09 BST
- Context: Audible autoplay still depends on browser policy, so the Nicole experience needed a
  deterministic way to start music on entry.
- Action: Replaced mount-time autoplay with a dedicated Nicole entry screen that opens the collage
  and starts `Arabella` from the first explicit click, while keeping the in-page play/pause control
  for later.
- Outcome: The Nicole remote now avoids browser autoplay roulette on first load and uses a single
  visible entry action to start the experience consistently.
- References: `services/web-nicole/src/exposed/BirthdayFeature.tsx`,
  `services/web-nicole/src/exposed/birthdayFeature.css`,
  `services/web-nicole/src/exposed/BirthdayFeature.test.tsx`

## 14:18 BST
- Context: The Nicole page needed to feel less like a landing page and more like a personal photo
  app, with the user-supplied six photos as the first set and a larger library behind them.
- Action: Rebuilt the Nicole remote around a shuffled photo-library view, copied the `nicole/`
  image set into service-local assets, moved the birthday message and short version into an app-like
  top panel, removed the social/render sections and chips, added a top-right play/pause control plus
  a `New set` shuffle action, and introduced per-photo description textareas backed by
  `localStorage`.
- Outcome: The first library view now opens on the requested six photos, subsequent refreshes rotate
  in a different mix from the broader shared-photo library, and typed descriptions persist across
  shuffles in the browser.
- References: `services/web-nicole/src/data.ts`,
  `services/web-nicole/src/exposed/BirthdayFeature.tsx`,
  `services/web-nicole/src/exposed/birthdayFeature.css`,
  `services/web-nicole/src/exposed/BirthdayFeature.test.tsx`,
  `services/web-nicole/src/assets/library/`

## 17:03 BST
- Context: The birthday message needed to come before the library in the user flow, with the card
  opening first and the library staying behind a second explicit action.
- Action: Split the Nicole experience into `Open card` and `Open library` steps, kept the
  play/pause control on the message card, and moved `New set` down into the library panel header.
- Outcome: The first click now opens the birthday card and starts the music, while the photo
  library only appears after the dedicated follow-up action.
- References: `services/web-nicole/src/exposed/BirthdayFeature.tsx`,
  `services/web-nicole/src/exposed/birthdayFeature.css`,
  `services/web-nicole/src/exposed/BirthdayFeature.test.tsx`

## 17:24 BST
- Context: The Nicole intro card still felt rough, the photo captions needed to become seeded copy
  instead of editable fields, and `New set` still risked repeating the same images immediately.
- Action: Refined the opening card into a centered single-card layout with Nicole's portrait on the
  front screen, removed the extra library gate so opening the card reveals the message plus photo
  library together, replaced textarea captions with hardcoded seeded descriptions, and changed the
  shuffle logic to avoid reusing the current image set when enough other photos are available.
- Outcome: `web-nicole` now opens into a cleaner intro card, the library captions are static and
  easy to edit in code later, and the next shuffle rotates to a genuinely different set. Local
  `pnpm --dir services/web-nicole test -- --run` and `pnpm --dir services/web-nicole build`
  passed, the refreshed `app-web-nicole` image rolled out successfully on `tm`, and the live host
  now serves `BirthdayFeature-CibqgjxQ.js`, `BirthdayFeature-DqYdzCdf.css`, and
  `IMG_6615-DAgi6fLO.jpg`.
- References: `services/web-nicole/src/data.ts`,
  `services/web-nicole/src/exposed/BirthdayFeature.tsx`,
  `services/web-nicole/src/exposed/birthdayFeature.css`,
  `services/web-nicole/src/exposed/BirthdayFeature.test.tsx`,
  `task docker:push:web-nicole`,
  `kubectl rollout status deployment/app-web-nicole -n app-system`

## 17:32 BST
- Context: The shell favicon also needed to update the installed phone PWA icon instead of only the
  desktop tab icon.
- Action: Generated square icon sizes from `services/web/public/frankicon.png`, switched the shell
  HTML favicon and apple-touch references to new `frankicon-*` filenames, updated the Vite PWA
  manifest icon entries to match, rebuilt `services/web`, and rolled `app-web` on `tm`.
- Outcome: The live shell now serves `frankicon-32x32.png`, `frankicon-180x180.png`,
  `frankicon-192x192.png`, and `frankicon-512x512.png`, and
  `https://eco.treehousehl.com/manifest.webmanifest` advertises the new PWA icon URLs for updated
  installs and refreshes.
- References: `services/web/index.html`, `services/web/vite.config.ts`,
  `services/web/public/frankicon-*.png`, `pnpm --dir services/web build`,
  `task docker:push:web`, `kubectl rollout status deployment/app-web -n app-system`

## 17:38 BST
- Context: The app branding needed to move fully from Eco/leaf to Frank, including the installed
  app name, the remaining sidebar logo usage, and legacy icon filenames that could still surface on
  phones or stale caches.
- Action: Renamed the shell title and PWA manifest name to `Frank`, changed the mobile drawer label
  and sidebar workspace logo to Frank branding, regenerated a tighter Frank crop for the icon set,
  and replaced both the new `frankicon-*` files and the older `logo.png`, `favicon.png`,
  `apple-touch-icon.png`, and `pwa-*.png` assets before rebuilding and rolling `app-web` on `tm`.
- Outcome: The live shell now serves `Frank | Home`, the manifest advertises `name=Frank` and
  `short_name=Frank`, and both the explicit PWA icon files and the older fallback asset filenames
  resolve to the updated Frank image.
- References: `services/web/index.html`, `services/web/vite.config.ts`,
  `services/web/src/features/workspaces/WorkspaceHeader.tsx`,
  `services/web/src/features/workspaces/WorkspaceRail.tsx`,
  `services/web/public/`, `pnpm --dir services/web test -- --run`,
  `pnpm --dir services/web build`, `task docker:push:web`,
  `kubectl rollout status deployment/app-web -n app-system`
