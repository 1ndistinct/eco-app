# Architecture

Start with `system-design.md` for the shell and service boundaries, then `runtime-topology.md`
for the deployed ingress shape, `ui-design.md` for the authenticated chrome behavior, and
`component-map.md` for the deployable package list.
