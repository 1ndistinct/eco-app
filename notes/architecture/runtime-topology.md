# Runtime Topology

The local default is k3d plus Helm. echo-agentic-todo-postfix-20260411210213 ships one app workload and one web workload, with one public host exposed through Traefik and path-based routing for frontend and backend traffic by default.
