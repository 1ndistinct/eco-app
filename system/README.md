# System

Runtime-owned identity and shared non-workspace guidance live here.

Use `system/agents/registry.md` for stable named identities and `system/teams/` for shared team knowledge, objectives, and workflow policies.
