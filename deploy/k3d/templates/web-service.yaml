apiVersion: v1
kind: Service
metadata:
  name: {{ include "app.webServiceName" . }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "app.webServiceName" . }}
  ports:
    - name: http
      port: {{ .Values.service.webPort }}
      targetPort: 80
