apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "app.appMigrationJobName" . }}
spec:
  backoffLimit: 6
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "app.appMigrationJobName" . }}
    spec:
      restartPolicy: OnFailure
      containers:
        - name: app-migrate
          image: "{{ .Values.images.app.repository }}:{{ .Values.images.app.tag }}"
          imagePullPolicy: {{ .Values.images.app.pullPolicy }}
          args:
            - migrate
          env:
            - name: DATABASE_URL
              value: {{ include "app.databaseURL" . | quote }}
