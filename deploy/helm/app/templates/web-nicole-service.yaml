apiVersion: v1
kind: Service
metadata:
  name: {{ include "app.webNicoleServiceName" . }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "app.webNicoleServiceName" . }}
  ports:
    - name: http
      port: {{ .Values.service.webPort }}
      targetPort: 80
