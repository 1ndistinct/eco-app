apiVersion: v1
kind: Service
metadata:
  name: {{ include "app.todoApiServiceName" . }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "app.todoApiServiceName" . }}
  ports:
    - name: http
      port: {{ .Values.service.appPort }}
      targetPort: {{ .Values.service.appPort }}
